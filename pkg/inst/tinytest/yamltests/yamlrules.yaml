rules:
-
  expr: x + y == z
  name: sumrule
  label: sum of x and y
  description: a looong description here
-
  expr: if (x > 0 ) y > 0
  name: conditional
  label: if x positive then y also
  description: | 
    a looong description here

