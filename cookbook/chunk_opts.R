if (knitr::is_latex_output()){
  knitr::opts_chunk$set(comment=NA
                      , size      = "tiny"
                      , tidy      = TRUE
                      , tidy.opts = list(width.cutoff=60))
  options(width=60)
}
